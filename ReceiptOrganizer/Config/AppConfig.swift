import Foundation

enum AppConfig {
    // Toggle to use a bundled/local sample image for OCR instead of the camera.
    // Set to false to use the live camera again.
    static let useLocalSampleReceipt: Bool = true
}

